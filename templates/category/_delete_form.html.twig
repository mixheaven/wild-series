<form method="post" action="{{ path('category_delete', { 'categoryName': category.name }) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.name) }}">
    <button class="btn">Delete</button>
</form>
