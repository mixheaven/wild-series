{% extends 'base.html.twig' %}

{% block title %}Actor{% endblock %}

{% block body %}
    <h1>Actor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ actor.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ actor.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('actor_index') }}">back to list</a>

  <br></br>

    <h3>filmograpie</h3>
    <ul>
        {% for program in actor.programs %}
           <a href="{{path('actor_index') }}"><li>{{ program.title }}</li></a>
        {% else %}
            <li>Cette acteur n'a joué dans aucunes series</li>
        {% endfor %}
    </ul>

{% endblock %}
